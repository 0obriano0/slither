#include <windows.h>
class UI
{
private:
	int windows_width;
	int windows_height;
	void SetColor(unsigned short ForeColor,unsigned short BackGroundColor);
protected:
	
public:
	void base();
	void GameMenu();
};
